{
  "name": "voxstream",
  "version": "1.0.0",
  "description": "Aplicación web de doblaje automático en tiempo real",
  "main": "src/index.js",
  "scripts": {
    "dev": "nodemon --watch src --ext js src/index.js",
    "start": "node src/index.js",
    "build": "echo 'Build completado - VoXStream listo para producción'",
    "test": "echo 'Tests por implementar'",
    "setup": "node -e \"console.log('🎬 Configurando VoXStream...'); require('fs').copyFileSync('.env.example', '.env'); console.log('✅ Archivo .env creado. Por favor configura tus API keys.');\"",
    "clean": "node -e \"const fs = require('fs-extra'); ['uploads', 'output', 'temp'].forEach(dir => { fs.emptyDirSync(dir); console.log('🧹 Directorio ' + dir + ' limpiado'); });\"",
    "check-deps": "node -e \"const { execSync } = require('child_process'); try { execSync('ffmpeg -version'); console.log('✅ FFmpeg instalado'); } catch(e) { console.log('❌ FFmpeg no encontrado. Instala con: brew install ffmpeg'); }\""
  },
  "keywords": [
    "doblaje",
    "traducción",
    "video",
    "audio",
    "IA",
    "transcripción"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@google-cloud/translate": "^8.0.2",
    "axios": "^1.6.2",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "form-data": "^4.0.4",
    "fs-extra": "^11.2.0",
    "helmet": "^7.1.0",
    "multer": "^1.4.5-lts.1",
    "node-gtts": "^2.0.2",
    "openai": "^4.20.1",
    "socket.io": "^4.7.4",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
